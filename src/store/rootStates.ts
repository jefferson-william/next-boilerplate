import INITIAL_STATE_AUTH from '~/store/Auth/state'
import INITIAL_STATE_REPO from '~/store/Repo/state'

const rootStates = {
  Auth: INITIAL_STATE_AUTH,
  Repo: INITIAL_STATE_REPO,
}

export default rootStates
